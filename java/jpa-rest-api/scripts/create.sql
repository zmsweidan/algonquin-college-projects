CREATE TABLE PRODUCT (ID INTEGER IDENTITY NOT NULL, INVENTORYCOUNT INTEGER, PRODUCTNAME VARCHAR, UNITPRICE DOUBLE, VERSION INTEGER, CREATED_DATE TIMESTAMP, UPDATED_DATE TIMESTAMP, PRIMARY KEY (ID))
CREATE TABLE CART (ID INTEGER IDENTITY NOT NULL, TOTALAMOUNT DOUBLE, TOTALDISCOUNT DOUBLE, VERSION INTEGER, CREATED_DATE TIMESTAMP, UPDATED_DATE TIMESTAMP, PRIMARY KEY (ID))
CREATE TABLE CART_ITEM (ID INTEGER IDENTITY NOT NULL, QUANTITY INTEGER, TOTALAMOUNT DOUBLE, VERSION INTEGER, CREATED_DATE TIMESTAMP, UPDATED_DATE TIMESTAMP, CART_ID INTEGER, PROUCT_ID INTEGER, PRIMARY KEY (ID))
ALTER TABLE CART_ITEM ADD CONSTRAINT FK_CART_ITEM_PROUCT_ID FOREIGN KEY (PROUCT_ID) REFERENCES product (ID)
ALTER TABLE CART_ITEM ADD CONSTRAINT FK_CART_ITEM_CART_ID FOREIGN KEY (CART_ID) REFERENCES CART (ID)